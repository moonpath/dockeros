networks:
  webos:
    name: webos
    driver: bridge
    ipam:
      config:
        - subnet: 10.20.40.0/24

services:
  webos:
    image: moonpath/webos:test
    container_name: webos
    hostname: linux
    shm_size: 16g
    privileged: true
    restart: unless-stopped
    stdin_open: true
    tty: true
    build:
      context: .
      dockerfile: Dockerfile.webos
      args:
        BASE_IMAGE: "ubuntu:24.04"
    environment:
      VNC_PORT: ${VNC_PORT:-7900}
      VNC_USER: ${VNC_USER:-user}
      VNC_PASSWD: ${VNC_PASSWD:-password}
      LC_ALL: ${LANG:-C.UTF-8}
      LANG: ${LANG:-C.UTF-8}
      TZ: ${TZ:-Etc/UTC}
    ports:
      - "8900:7900/tcp"  
    extra_hosts:
      - host.docker.internal:host-gateway
    networks:
      - webos
    deploy:
      resources:
        limits:
          memory: 8g
